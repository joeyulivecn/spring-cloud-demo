spring:
  application:
    name: cfg-server
  profiles:
    active: native # use local file system to store config

  ### use git
#  cloud:
#    config:
#      server:
#        git:
#          uri: http://gitee.com/xxx/xxx.git
server:
  port: 8888


# visit http://localhost:8888/cfg-server/dev/